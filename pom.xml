<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.workshop.flink</groupId>
    <artifactId>flink-workshop</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Flink Workshop: Reliability, Failover and Deduplication</name>

    <modules>
        <module>common</module>
        <module>scenario-01-baseline-at-least-once</module>
        <module>scenario-02-exactly-once-kafka</module>
        <module>scenario-03-external-sink-duplicates</module>
        <module>scenario-04-upstream-true-duplicates</module>
        <module>scenario-05-rescaling-replay</module>
        <module>scenario-06-paimon-lakehouse</module>
        <module>scenario-07-stream-stream-joins</module>
        <module>scenario-08-stream-table-joins</module>
        <module>scenario-09-multiway-join-recovery</module>
        <!-- Scenarios 11, 12, 14: SQL-only, no Maven module -->
        <module>scenario-13-fluss-partial-updates</module>
        <module>scenario-15-fluss-java-clients</module>
        <module>scenario-16-fluss-paimon-tiered-spark</module>
    </modules>

    <properties>
        <java.version>11</java.version>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <flink.version>1.20.4</flink.version>
        <flink.kafka.connector.version>3.4.0-1.20</flink.kafka.connector.version>
        <flink.jdbc.connector.version>3.3.0-1.20</flink.jdbc.connector.version>
        <paimon.version>1.0.1</paimon.version>
        <fluss.version>0.9.1-incubating</fluss.version>
        <spark.version>3.5.5</spark.version>
        <spark.scala.binary.version>2.12</spark.scala.binary.version>
        <kafka.clients.version>3.7.0</kafka.clients.version>
        <postgresql.version>42.7.3</postgresql.version>
        <caffeine.version>3.1.8</caffeine.version>
        <jackson.version>2.17.1</jackson.version>
        <log4j.version>2.23.1</log4j.version>
        <slf4j.version>2.0.13</slf4j.version>
        <junit.version>5.10.2</junit.version>
        <assertj.version>3.25.3</assertj.version>

        <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
        <maven.shade.plugin.version>3.6.0</maven.shade.plugin.version>
        <maven.surefire.plugin.version>3.2.5</maven.surefire.plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal -->
            <dependency>
                <groupId>com.workshop.flink</groupId>
                <artifactId>flink-workshop-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Flink core — provided by cluster or local mini-cluster -->
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-streaming-java</artifactId>
                <version>${flink.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-clients</artifactId>
                <version>${flink.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Flink connectors — bundled in fat JAR -->
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-connector-kafka</artifactId>
                <version>${flink.kafka.connector.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-connector-base</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-connector-datagen</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-connector-jdbc</artifactId>
                <version>${flink.jdbc.connector.version}</version>
            </dependency>

            <!-- Flink Table API — needed by scenario-06 (StreamTableEnvironment) -->
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-table-api-java-bridge</artifactId>
                <version>${flink.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-table-planner-loader</artifactId>
                <version>${flink.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-table-runtime</artifactId>
                <version>${flink.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Apache Paimon — lakehouse table format (scenario-06) -->
            <dependency>
                <groupId>org.apache.paimon</groupId>
                <artifactId>paimon-flink-1.20</artifactId>
                <version>${paimon.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Apache Fluss — streaming storage (scenarios 11-16). -->
            <!-- fluss-client: pure Java SDK for point-in-time lookups -->
            <dependency>
                <groupId>org.apache.fluss</groupId>
                <artifactId>fluss-client</artifactId>
                <version>${fluss.version}</version>
            </dependency>
            <!-- fluss-common: shared types (Row, schema) -->
            <dependency>
                <groupId>org.apache.fluss</groupId>
                <artifactId>fluss-common</artifactId>
                <version>${fluss.version}</version>
            </dependency>
            <!-- Flink connector — provided at runtime by the workshop/flink-fluss image -->
            <dependency>
                <groupId>org.apache.fluss</groupId>
                <artifactId>fluss-flink-1.20</artifactId>
                <version>${fluss.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Apache Spark — used by scenario-16 -->
            <dependency>
                <groupId>org.apache.spark</groupId>
                <artifactId>spark-core_${spark.scala.binary.version}</artifactId>
                <version>${spark.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.spark</groupId>
                <artifactId>spark-sql_${spark.scala.binary.version}</artifactId>
                <version>${spark.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.paimon</groupId>
                <artifactId>paimon-spark-3.5</artifactId>
                <version>${paimon.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Kafka native client (for DuplicateEventProducer in scenario-04) -->
            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-clients</artifactId>
                <version>${kafka.clients.version}</version>
            </dependency>

            <!-- Serialization -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <!-- Database -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>

            <!-- Caching for async lookup joins (scenarios 08/09) -->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- Logging -->
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-slf4j2-impl</artifactId>
                <version>${log4j.version}</version>
                <scope>runtime</scope>
            </dependency>
            <!-- log4j-api must be compile scope: it provides Logger/LogManager for source code -->
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-api</artifactId>
                <version>${log4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-core</artifactId>
                <version>${log4j.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- Test -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <compilerArgs>
                            <arg>--add-opens=java.base/java.util=ALL-UNNAMED</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.plugin.version}</version>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${maven.shade.plugin.version}</version>
                    <configuration>
                        <filters>
                            <filter>
                                <artifact>*:*</artifact>
                                <excludes>
                                    <exclude>META-INF/*.SF</exclude>
                                    <exclude>META-INF/*.DSA</exclude>
                                    <exclude>META-INF/*.RSA</exclude>
                                    <exclude>META-INF/MANIFEST.MF</exclude>
                                </excludes>
                            </filter>
                        </filters>
                        <artifactSet>
                            <excludes>
                                <exclude>org.apache.flink:flink-streaming-java</exclude>
                                <exclude>org.apache.flink:flink-clients</exclude>
                            </excludes>
                        </artifactSet>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
