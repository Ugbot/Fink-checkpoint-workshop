version: "3.8"

# Run with: podman-compose up -d
# Flink Web UI:  http://localhost:18081
# Kafka UI:      (not included — use verify scripts or kcat)
# Kafka broker:  localhost:19092
# PostgreSQL:    localhost:15432  (user: workshop / pass: workshop / db: workshop)

services:

  kafka:
    image: docker.io/apache/kafka:4.2.0
    container_name: workshop-kafka
    ports:
      - "19092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      # Two listeners: PLAINTEXT_HOST for jobs running on the host, PLAINTEXT for container-to-container
      KAFKA_LISTENERS: PLAINTEXT_HOST://:9092,PLAINTEXT://:9093,CONTROLLER://:9094
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT_HOST://localhost:19092,PLAINTEXT://workshop-kafka:9093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT_HOST:PLAINTEXT,PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9094
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      # Single-broker overrides required for Kafka transactions (exactly-once)
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_MAX_TIMEOUT_MS: 3600000
      KAFKA_LOG_RETENTION_HOURS: 168
      KAFKA_NUM_PARTITIONS: 4
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"
    networks:
      - workshop-net

  postgres:
    image: docker.io/library/postgres:16
    container_name: workshop-postgres
    ports:
      - "15432:5432"
    environment:
      POSTGRES_USER: workshop
      POSTGRES_PASSWORD: workshop
      POSTGRES_DB: workshop
    volumes:
      - ./scripts/init-postgres.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - postgres-data:/var/lib/postgresql/data
    networks:
      - workshop-net

  flink-jobmanager:
    image: docker.io/library/flink:1.20
    container_name: workshop-jobmanager
    command: jobmanager
    ports:
      - "18081:8081"
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: workshop-jobmanager
        taskmanager.numberOfTaskSlots: 8
        parallelism.default: 2
        state.backend: filesystem
        state.checkpoints.dir: file:///opt/flink/checkpoints
        state.savepoints.dir: file:///opt/flink/savepoints
        execution.checkpointing.externalized-checkpoint-retention: RETAIN_ON_CANCELLATION
    volumes:
      - flink-checkpoints:/opt/flink/checkpoints
      - flink-savepoints:/opt/flink/savepoints
    networks:
      - workshop-net

  flink-taskmanager:
    image: docker.io/library/flink:1.20
    container_name: workshop-taskmanager
    command: taskmanager
    depends_on:
      - flink-jobmanager
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: workshop-jobmanager
        taskmanager.numberOfTaskSlots: 8
    volumes:
      - flink-checkpoints:/opt/flink/checkpoints
      - flink-savepoints:/opt/flink/savepoints
    networks:
      - workshop-net

volumes:
  postgres-data:
  flink-checkpoints:
  flink-savepoints:

networks:
  workshop-net:
    driver: bridge
